version: '3.4'

services:
  javaglqlreactreduxrtkexperiment:
    image: javaglqlreactreduxrtkexperiment
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 8080:8080
